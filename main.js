// Apertura menu hambuger utilizzando toggle ed i vari parametri
$('.fa-bars').click(
  function(){
    $('.hamburger-menu').toggle('fast', 'swing');
  }
)

// Chiusura menu hambuger utilizzando toggle ed i vari parametri
$('.fa-times').click(
  function(){
    $('.hamburger-menu').toggle('fast', 'swing');
  }
)
